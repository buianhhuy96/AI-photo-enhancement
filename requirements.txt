# WindowSeat Reflection Removal - Local Application
# Versions matched to official repo: huawei-bayerlab/windowseat-reflection-removal

# Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0

# ML / Inference (versions critical - model classes require these minimums)
torch>=2.8.0
torchvision>=0.23.0
diffusers>=0.35.0
transformers>=4.57.0
accelerate>=1.10.0
safetensors>=0.7.0
huggingface_hub>=0.36.0
peft>=0.17.0
bitsandbytes>=0.47.0
imageio>=2.37.0
numpy>=2.0.0
Pillow>=12.0.0

# UI (Desktop app)
customtkinter>=5.2.0
Pillow>=12.0.0

# UI (Web mode, optional — for --web flag)
gradio>=4.0.0
